rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Checks if the user is signed in.
     * @path N/A
     * @allow N/A
     * @deny N/A
     * @principle Authentication is required for access.
     */
    function isSignedIn() {
      return request.auth != null;
    }

    /**
     * @description Checks if the authenticated user's UID matches the provided userId.
     * @path N/A
     * @allow N/A
     * @deny N/A
     * @principle Enforces user ownership.
     */
    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    /**
     * @description Checks if the authenticated user is an existing owner of the document.
     * @path N/A
     * @allow N/A
     * @deny N/A
     * @principle Enforces user ownership and document existence.
     */
    function isExistingOwner(userId) {
      return isOwner(userId) && resource != null;
    }

    /**
     * @description Matches the document that maps user UIDs to tenant IDs.
     * @path /users/{userId}
     * @allow (create) An authenticated user creates their own UserTenant document.
     * @deny (create) A user attempts to create a UserTenant document for another user.
     * @principle Ensures that a user can only create their own UserTenant document.
     */
    match /users/{userId} {
      allow get: if isOwner(userId);
      allow list: if false;
      allow create: if isOwner(userId);
      allow update: if isOwner(userId);
      allow delete: if isOwner(userId);
    }

    /**
     * @description Matches a specific tenant document.
     * @path /tenants/{tenantId}
     * @allow (get) Any authenticated user can get a tenant.
     * @deny (create) No one can create tenant documents through the rules (likely created via script).
     * @principle Restricts tenant creation, allows authenticated reads.
     */
    match /tenants/{tenantId} {
      allow get: if isSignedIn();
      allow list: if false;
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Matches a category document within a tenant.
     * @path /tenants/{tenantId}/categories/{categoryId}
     * @allow (get, list) Any authenticated user can read categories.
     * @allow (create, update, delete) No one can modify category documents through the rules (likely created via script).
     * @deny (create) Unauthenticated user tries to create a category.
     * @principle Restricts category modification, allows authenticated reads.
     */
    match /tenants/{tenantId}/categories/{categoryId} {
      allow get, list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Matches a product document within a tenant.
     * @path /tenants/{tenantId}/products/{productId}
     * @allow (get, list) Any authenticated user can read products.
     * @allow (create, update, delete) No one can modify product documents through the rules (likely created via script).
     * @deny (create) Unauthenticated user tries to create a product.
     * @principle Restricts product modification, allows authenticated reads.
     */
    match /tenants/{tenantId}/products/{productId} {
      allow get, list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Matches a customer document within a tenant.
     * @path /tenants/{tenantId}/customers/{customerId}
     * @allow (get, list) Any authenticated user can read customers.
     * @allow (create, update, delete) No one can modify customer documents through the rules (likely created via script).
     * @deny (create) Unauthenticated user tries to create a customer.
     * @principle Restricts customer modification, allows authenticated reads.
     */
    match /tenants/{tenantId}/customers/{customerId} {
      allow get, list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Matches an order document within a tenant.
     * @path /tenants/{tenantId}/orders/{orderId}
     * @allow (get, list) Any authenticated user can read orders.
     * @allow (create, update, delete) No one can modify order documents through the rules (likely created via script).
     * @deny (create) Unauthenticated user tries to create an order.
     * @principle Restricts order modification, allows authenticated reads.
     */
    match /tenants/{tenantId}/orders/{orderId} {
      allow get, list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Matches a table document within a tenant.
     * @path /tenants/{tenantId}/tables/{tableId}
     * @allow (get, list) Any authenticated user can read tables.
     * @allow (create, update, delete) No one can modify table documents through the rules (likely created via script).
     * @deny (create) Unauthenticated user tries to create a table.
     * @principle Restricts table modification, allows authenticated reads.
     */
    match /tenants/{tenantId}/tables/{tableId} {
      allow get, list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

     /**
     * @description Matches a KDS order document within a tenant.
     * @path /tenants/{tenantId}/kdsOrders/{kdsId}
     * @allow (get, list) Any authenticated user can read KDS orders.
     * @allow (create, update, delete) No one can modify KDS order documents through the rules (likely created via script).
     * @deny (create) Unauthenticated user tries to create a KDS order.
     * @principle Restricts KDS order modification, allows authenticated reads.
     */
    match /tenants/{tenantId}/kdsOrders/{kdsId} {
      allow get, list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Matches a user document within a tenant.
     * @path /tenants/{tenantId}/users/{userId}
     * @allow (get, list) Any authenticated user can read users within a tenant.
     * @allow (create, update, delete) No one can modify user documents through the rules (likely created via script).
     * @deny (create) Unauthenticated user tries to create a user.
     * @principle Restricts user modification within a tenant, allows authenticated reads.
     */
    match /tenants/{tenantId}/users/{userId} {
      allow get, list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Matches a payment document within a tenant.
     * @path /tenants/{tenantId}/payments/{paymentId}
     * @allow (get, list) Any authenticated user can read payments.
     * @allow (create, update, delete) No one can modify payment documents through the rules (likely created via script).
     * @deny (create) Unauthenticated user tries to create a payment.
     * @principle Restricts payment modification, allows authenticated reads.
     */
    match /tenants/{tenantId}/payments/{paymentId} {
      allow get, list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Matches the general settings document within a tenant.
     * @path /tenants/{tenantId}/settings/general
     * @allow (get) Any authenticated user can read general settings.
     * @allow (create, update, delete) No one can modify general settings documents through the rules (likely created via script).
     * @deny (create) Unauthenticated user tries to create general settings.
     * @principle Restricts general settings modification, allows authenticated reads.
     */
    match /tenants/{tenantId}/settings/general {
      allow get: if isSignedIn();
      allow list: if false;
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Matches the module settings document within a tenant.
     * @path /tenants/{tenantId}/settings/modules
     * @allow (get) Any authenticated user can read module settings.
     * @allow (create, update, delete) No one can modify module settings documents through the rules (likely created via script).
     * @deny (create) Unauthenticated user tries to create module settings.
     * @principle Restricts module settings modification, allows authenticated reads.
     */
    match /tenants/{tenantId}/settings/modules {
      allow get: if isSignedIn();
      allow list: if false;
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Matches the automation rules settings document within a tenant.
     * @path /tenants/{tenantId}/settings/automationRules
     * @allow (get) Any authenticated user can read automation rules settings.
     * @allow (create, update, delete) No one can modify automation rules settings documents through the rules (likely created via script).
     * @deny (create) Unauthenticated user tries to create automation rules settings.
     * @principle Restricts automation rules settings modification, allows authenticated reads.
     */
    match /tenants/{tenantId}/settings/automationRules {
      allow get: if isSignedIn();
      allow list: if false;
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Matches the RKSV configuration document for a tenant.
     * @path /tenants/{tenantId}/rksvConfig
     * @allow (get) Any authenticated user can read the RKSV config.
     * @allow (create, update, delete) No one can modify the RKSV config through the rules (likely created via script).
     * @deny (create) Unauthenticated user tries to create the RKSV config.
     * @principle Restricts RKSV config modification, allows authenticated reads.
     */
    match /tenants/{tenantId}/rksvConfig {
      allow get: if isSignedIn();
      allow list: if false;
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Matches a signature document within a tenant.
     * @path /tenants/{tenantId}/signatures/{signatureId}
     * @allow (get, list) Any authenticated user can read signatures.
     * @allow (create, update, delete) No one can modify signature documents through the rules (likely created via script).
     * @deny (create) Unauthenticated user tries to create a signature.
     * @principle Restricts signature modification, allows authenticated reads.
     */
    match /tenants/{tenantId}/signatures/{signatureId} {
      allow get, list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Matches a Z-Report document within a tenant.
     * @path /tenants/{tenantId}/zReports/{reportId}
     * @allow (get, list) Any authenticated user can read Z-Reports.
     * @allow (create, update, delete) No one can modify Z-Report documents through the rules (likely created via script).
     * @deny (create) Unauthenticated user tries to create a Z-Report.
     * @principle Restricts Z-Report modification, allows authenticated reads.
     */
    match /tenants/{tenantId}/zReports/{reportId} {
      allow get, list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Matches an integration document within a tenant.
     * @path /tenants/{tenantId}/integrations/{platformId}
     * @allow (get, list) Any authenticated user can read integrations.
     * @allow (create, update, delete) No one can modify integration documents through the rules (likely created via script).
     * @deny (create) Unauthenticated user tries to create an integration.
     * @principle Restricts integration modification, allows authenticated reads.
     */
    match /tenants/{tenantId}/integrations/{platformId} {
      allow get, list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Matches a cash register document within a tenant.
     * @path /tenants/{tenantId}/cashRegisters/{registerId}
     * @allow (get, list) Any authenticated user can read cash registers.
     * @allow (create, update, delete) No one can modify cash register documents through the rules (likely created via script).
     * @deny (create) Unauthenticated user tries to create a cash register.
     * @principle Restricts cash register modification, allows authenticated reads.
     */
    match /tenants/{tenantId}/cashRegisters/{registerId} {
      allow get, list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Matches a report document within a tenant.
     * @path /tenants/{tenantId}/reports/{reportId}
     * @allow (get, list) Any authenticated user can read reports.
     * @allow (create, update, delete) No one can modify report documents through the rules (likely created via script).
     * @deny (create) Unauthenticated user tries to create a report.
     * @principle Restricts report modification, allows authenticated reads.
     */
    match /tenants/{tenantId}/reports/{reportId} {
      allow get, list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Matches a log document within a tenant.
     * @path /tenants/{tenantId}/finanzOnlineLogs/{logId}
     * @allow (get, list) Any authenticated user can read logs.
     * @allow (create, update, delete) No one can modify log documents through the rules (likely created via script).
     * @deny (create) Unauthenticated user tries to create a log.
     * @principle Restricts log modification, allows authenticated reads.
     */
    match /tenants/{tenantId}/finanzOnlineLogs/{logId} {
      allow get, list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Matches a system error document within a tenant.
     * @path /tenants/{tenantId}/systemErrors/{errorId}
     * @allow (get, list) Any authenticated user can read system errors.
     * @allow (create, update, delete) No one can modify system error documents through the rules (likely created via script).
     * @deny (create) Unauthenticated user tries to create a system error.
     * @principle Restricts system error modification, allows authenticated reads.
     */
    match /tenants/{tenantId}/systemErrors/{errorId} {
      allow get, list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Matches a plan document in the global collection.
     * @path /global/plans/{planId}
     * @allow (get, list) Anyone can read subscription plans.
     * @allow (create, update, delete) No one can modify subscription plans through the rules (likely created via script).
     * @deny (create) Unauthenticated user tries to create a plan.
     * @principle Allows public read access to subscription plans, restricts modification.
     */
    match /global/plans/{planId} {
      allow get, list: if true;
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Matches a message document within an AI chat in a tenant.
     * @path /tenants/{tenantId}/aiChats/{chatId}/messages/{messageId}
     * @allow (get, list) Any authenticated user can read AI chat messages.
     * @allow (create, update, delete) No one can modify AI chat messages through the rules (likely created via script).
     * @deny (create) Unauthenticated user tries to create an AI chat message.
     * @principle Restricts AI chat message modification, allows authenticated reads.
     */
    match /tenants/{tenantId}/aiChats/{chatId}/messages/{messageId} {
      allow get, list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Matches a POS order document within a tenant.
     * @path /tenants/{tenantId}/posOrders/{orderId}
     * @allow (get, list) Any authenticated user can read POS orders.
     * @allow (create, update, delete) No one can modify POS order documents through the rules (likely created via script).
     * @deny (create) Unauthenticated user tries to create a POS order.
     * @principle Restricts POS order modification, allows authenticated reads.
     */
    match /tenants/{tenantId}/posOrders/{orderId} {
      allow get, list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Matches an audit log document within a tenant.
     * @path /tenants/{tenantId}/auditLogs/{logId}
     * @allow (get, list) Any authenticated user can read audit logs.
     * @allow (create, update, delete) No one can modify audit log documents through the rules (likely created via script).
     * @deny (create) Unauthenticated user tries to create an audit log.
     * @principle Restricts audit log modification, allows authenticated reads.
     */
    match /tenants/{tenantId}/auditLogs/{logId} {
      allow get, list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Matches a notification document within a tenant.
     * @path /tenants/{tenantId}/notifications/{notificationId}
     * @allow (get, list) Any authenticated user can read notifications.
     * @allow (create, update, delete) No one can modify notification documents through the rules (likely created via script).
     * @deny (create) Unauthenticated user tries to create a notification.
     * @principle Restricts notification modification, allows authenticated reads.
     */
    match /tenants/{tenantId}/notifications/{notificationId} {
      allow get, list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Matches a mail document in the top-level collection.
     * @path /mail/{mailId}
     * @allow (get, list) Any authenticated user can read mail.
     * @allow (create, update, delete) No one can modify mail documents through the rules (likely created via script).
     * @deny (create) Unauthenticated user tries to create a mail.
     */
    match /mail/{mailId} {
      allow get, list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }
  }
}